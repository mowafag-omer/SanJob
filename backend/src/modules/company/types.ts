export type companyProps = {
  id?: number;
  name: string;
  location: string;
  sector: string;
  presentation: string;
  founding_year: string;
  employees: number
  website: string
  user: { id: number }
}